# UI mockups

This folder is intentionally lightweight because Nucleus is currently maintained as a reusable NuGet package, not as a standalone UI application.

If the repository later gains a demo UI or sample host, add one subfolder per mockup and keep the related intent documented in [`docs/specs/ui.md`](../specs/ui.md).
